- name: Launch database
  docker_container:
    name: database
    image: rayanzeriri/tp1-db:latest
    env:
      POSTGRES_PASSWORD: "pwd"
    volumes:
      - db-volume:/var/lib/postgresql/data
    networks:
      - name: my-network
  environment:
    ansible_python_interpreter: /opt/docker_venv/bin/python