- name: Launch app
  docker_container:
    name: simple-api
    image: rayanzeriri/tp1-backend:latest
    env:
      DATABASE_HOST: "database"
      DATABASE_PASSWORD: "pwd"
    networks:
      - name: my-network
  environment:
    ansible_python_interpreter: /opt/docker_venv/bin/python